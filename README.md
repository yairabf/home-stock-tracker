# Home Stock Tracker

Track your home inventory and stock levels.

## Getting Started

This project is in early development and uses NestJS.

## Commands

- Development server: `npm run start:dev`
- Build: `npm run build`
- Production server: `npm run start:prod`
- Lint: `npm run lint`
- Tests: `npm run test`
- End-to-end tests: `npm run test:e2e`

## Deployment

The production Docker and Compose procedure is documented in
[`docs/deployment.md`](docs/deployment.md). It covers configuration, migrations,
startup, health checks, authenticated smoke tests, restarts, logs, and rollback.

## Blueprint

This project uses the [AI Blueprint](https://github.com/anthropics/ai-blueprint) workflow for AI-assisted development. See `AGENTS.md` for project commands and conventions.
